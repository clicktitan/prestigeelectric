# prestigeelectric

## Claude Code plugins

This project includes the official `frontend-design` plugin as a project-local Claude Code plugin under:

```text
.claude/skills/frontend-design/
```

Start Claude Code from the repository root so it auto-discovers the project-scoped plugin and uses the `frontend-design` skill for frontend/UI implementation prompts.
