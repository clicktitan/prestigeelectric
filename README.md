# Prestige Electric

A simple static website starter for Prestige Electric.

## Run locally

From the repository root:

```bash
python3 -m http.server 8000 --bind 0.0.0.0
```

Then open:

```text
http://localhost:8000
```

Edit `index.html` and `styles.css`, then refresh the browser to see changes.
